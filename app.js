const modal=document.querySelector('#modal');const title=document.querySelector('#modal-title');const copy=document.querySelector('#modal-copy');const menu=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');
function openModal(type,course){title.textContent=type==='login'?'Welcome back':course?`Start ${course}`:'Create your free account';copy.textContent=type==='login'?'Log in to continue your learning journey.':course?`Create an account to begin ${course}.`:'Start learning practical endpoint management skills today.';modal.classList.add('open');modal.setAttribute('aria-hidden','false');modal.querySelector('input').focus()}
function closeModal(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true')}
document.querySelectorAll('[data-action]').forEach(button=>button.addEventListener('click',()=>openModal(button.dataset.action)));document.querySelectorAll('[data-course]').forEach(button=>button.addEventListener('click',()=>openModal('signup',button.dataset.course)));document.querySelector('.modal-close').addEventListener('click',closeModal);modal.addEventListener('click',event=>{if(event.target===modal)closeModal()});document.addEventListener('keydown',event=>{if(event.key==='Escape')closeModal()});menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.main-nav a').forEach(link=>link.addEventListener('click',()=>nav.classList.remove('open')));document.querySelector('#auth-form').addEventListener('submit',event=>{event.preventDefault();const button=event.currentTarget.querySelector('button');button.textContent='You’re on your way! ✓';button.disabled=true;setTimeout(()=>{closeModal();button.textContent='Continue learning →';button.disabled=false;event.currentTarget.reset()},1200)});
